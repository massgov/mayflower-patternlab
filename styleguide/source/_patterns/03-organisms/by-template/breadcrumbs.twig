<section class="ma__breadcrumbs {{breadcrumb.class}}">
  <div class="ma__breadcrumbs__container">
    <ul>
      {% for breadcrumb in breadcrumbs %}
      <li><a href="{{breadcrumb.href}}">{{breadcrumb.text}}</a></li>
      {% endfor %}
    </ul>
  </div>
</section>