{%
  set iconRole = iconRole ? iconRole : 'presentation'
%}
<svg focusable="false" role="{{ iconRole }}" class="svg-flickr">
  <use xlink:href="/{{ directory }}/images/svg-sprite.svg#flickr"></use>
</svg>
