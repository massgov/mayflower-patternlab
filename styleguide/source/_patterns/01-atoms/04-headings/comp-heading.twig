{% if compHeading.color %}
  {% set modifier = "ma__comp-heading--" ~ compHeading.color %}
{% endif %}

{% if compHeading.sub %}
  <h3 {{tagType}} class="ma__comp-heading {{modifier}}" {% if compHeading.id %}id="{{compHeading.id}}"{% endif %}>
    {{ compHeading.title }}
  </h3>
{% else %}
  <h2 {{tagType}} class="ma__comp-heading {{modifier}}" {% if compHeading.id %}id="{{compHeading.id}}"{% endif %}>
    {{ compHeading.title }}
  </h2>
{% endif %}

