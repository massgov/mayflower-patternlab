{# use the level passed, the gloval level, or the default #}
{% set headingLevel = sidebarHeading.level ? : (globalLevel ? : 3) %}

<h{{ headingLevel }} class="ma__sidebar-heading">{{sidebarHeading.title}}</h{{ headingLevel }}>